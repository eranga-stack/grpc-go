syntax = "proto3";

package calculator;

option go_package = "github.com/eranga-stack/grpc-go/calculator/proto";

import "sum.proto";

service CalculatorService {
    rpc Sum(SumRequest) returns (SumResponse);
}